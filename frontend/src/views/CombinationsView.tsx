import { CombosWorkspace } from '../components/studio/CombosWorkspace'

/** Combination Therapy & Synthetic Lethality workspace (`/combinations`, `/combos`). */
export function CombinationsView() {
  return <CombosWorkspace />
}
