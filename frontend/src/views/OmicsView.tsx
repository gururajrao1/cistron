import { OmicsUploader } from '../components/studio/OmicsUploader'

/** Phase 2 — differential omics upload → y₀-conditioned Studio simulation. */
export function OmicsView() {
  return <OmicsUploader />
}
